# Shared specialist memory

Keep only concise, durable, non-secret facts that should apply across this specialist's threads.

Do not store credentials, contact details, account identifiers, private URLs, source documents, conversation transcripts, or short-lived task state here.
